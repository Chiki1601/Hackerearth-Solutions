#include<bits/stdc++.h>
using namespace std;
 
int main()
{ 
	  ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    int n;
    cin >> n;
    if(n == 1)
		    cout << 2 << endl;
    else if(n == 2)
		    cout << 1 << endl;
    else
		    cout << 0 << endl;
}
